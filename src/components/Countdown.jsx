import { useEffect, useState } from 'react'; const Countdown = () => { const launchDate = new Date('2025-10-01T00:00:00+13:00').getTime(); const [timeLeft, setTimeLeft] = useState(launchDate - Date.now()); useEffect(() => { const timer = setInterval(() => setTimeLeft(launchDate - Date.now()), 1000); return () => clearInterval(timer); }, []); const formatTime = ms => { const s = Math.floor(ms / 1000); const d = Math.floor(s / 86400); const h = Math.floor((s % 86400) / 3600); const m = Math.floor((s % 3600) / 60); return `${d}d ${h}h ${m}m ${s % 60}s`; }; return <div>🚀 Launching in: {formatTime(timeLeft)}</div>; }; export default Countdown;